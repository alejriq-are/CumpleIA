# F1.23B — Verifier calibrado de selector

**Estado:** PASS

Se añadió `rat-active-organization-selector-v2` sin cambiar el verifier
histórico de F1.22. El perfil mide helper exportado, validación mediante
membresías, fallback seguro, URL, propagación y accesibilidad, sin exigir un
nombre concreto de función.

El baseline fue rechazado y el workspace retenido de Claude r2 aprobó 4/4 en
modo sólo lectura. Suite trusted: `23 passed, 4 skipped`.

**Siguiente paso:** F1.23C, decidir si se usa este contrato calibrado para una
nueva ronda comparativa o sólo como corrección metodológica.
