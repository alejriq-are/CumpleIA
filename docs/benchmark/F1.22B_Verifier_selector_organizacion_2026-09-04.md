# F1.22B — Verifier trusted: selector de organización activa

**Fecha:** 2026-09-04  
**Estado:** PASS

Se incorporó el perfil `rat-active-organization-selector-v1`, encadenado a
`rat-default`. El contrato se ejecuta sin red, con workspace candidato de sólo
lectura y filesystem efímero. Comprueba el helper de selección validada, el
uso de `searchParams.organization` en ambas páginas, la ausencia de selección
implícita y un selector accesible que actualiza el contexto URL.

El baseline fue rechazado con 4 fallos esperados: no existe helper, las páginas
no leen el parámetro y Organización usa `organizaciones[0]`. Una solución
dorada temporal aprobó 6/6; su worktree fue eliminado inmediatamente. La
suite trusted terminó con `23 passed, 3 skipped`.

**Siguiente paso:** F1.22C — fijar baseline, hashes y candidatos antes de
autorizar una nueva ronda.
