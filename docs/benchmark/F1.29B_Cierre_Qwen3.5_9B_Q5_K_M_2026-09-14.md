# F1.29B — Cierre Qwen3.5-9B Q5_K_M

Fecha: 2026-09-14. **CERRADO — Gate 1 PASS / Gate 2 FAIL / STOP.**

## Resultado por gate

1. **Gate 1 — tool_use directo PASS.** `/v1/messages` devuelve un bloque
   estructurado `type: tool_use`, `name: read_file`,
   `input: {"file_path":"README.md"}` y `stop_reason: tool_use`.
   No demuestra por sí solo ejecución de herramientas en Claude Code.
2. **Template stock incompatible con Claude Code.** Los mensajes `system`
   intercalados provocan HTTP 500: `System message must be at the beginning.`
3. **Patch mínimo elimina HTTP 500.** Se copia el template específico
   `models/templates/Qwen3.5-4B.jinja` a `qwen35-claude-code.jinja` y se cambia
   sólo el rechazo de `system` intercalado por su serialización:

   ```diff
   - {{- raise_exception('System message must be at the beginning.') }}
   + {{- '<|im_start|>system\n' + content + '<|im_end|>' + '\n' }}
   ```

4. **Gate 2 — Claude Code real FAIL.** Tras el patch no ejecuta `Read` ni
   devuelve el contenido del probe. El primer resultado JSON termina con tres
   backticks. El reintento explícito `--model local --allowedTools Read
   --output-format json` inventa que el archivo no existe, sin comprobarlo.
   En ambos: `iterations: []`, `num_turns: 1`, `EXIT=0`. La salida normal del
   proceso no equivale al ciclo herramienta → resultado → respuesta requerido.

**STOP: no se ejecutan mini-agentic, performance ni RAT.** No se continúan
adaptaciones de template en esta evaluación. El fallo corresponde al stack y
configuración evaluados, no prueba imposibilidad en cualquier integración futura.
Se ratifica **Qwen3.6-35B-A3B IQ4_XS** como principal seleccionado en
[F1.28](F1.28_Cierre_seleccion_modelos_locales_2026-09-11.md), conservando F1.25-B FAIL.
La variante Q5_K_M de F1.29B no reescribe la evaluación Q4_K_M de F1.24.

## Configuración y trazabilidad

Fuente: conversación [Estrategia LLM local y nube](https://chatgpt.com/c/6a8f5468-fe5c-83e9-985a-d3929d0a3dfb)
y solicitud de cierre. Gate 1: turno `7b868c27-982f-4387-bd07-afa59113d18c`;
diff del patch: `d7748cb8-2ec1-49de-9c31-d9e196269cad`;
Gate 2 final: `2f8c5b1f-d796-4224-864c-109f74e83fa4`.

- Runtime: `~/runtime/llama.cpp-mainline-f127b`.
- Modelo: `/mnt/d/CumpleIA-LLM/models/Qwen3.5-9B-Q5_K_M/Qwen_Qwen3.5-9B-Q5_K_M.gguf`.
- Servidor: `--jinja -ngl 99 --ctx-size 32768 --flash-attn on --reasoning off
  --no-reasoning-preserve -np 1 --host 127.0.0.1 --port 8080`.
  Después del patch: `--chat-template-file ./qwen35-claude-code.jinja`.
- Gate 1: POST `/v1/messages`, modelo `local`, `max_tokens: 256`, herramienta
  `read_file` con `file_path` obligatorio. Respuesta
  `chatcmpl-NYBW8fhLIXparAC2z1zR5dcyGPwsEPJY`, 288 tokens de entrada y 27 de salida.
- Probe reportado: `~/projects/CumpleIA/.f129b-read-probe.txt`, cadena aleatoria
  con prefijo `QWEN35-F129B-`. No se inventa ni se reconstruye su UUID.
- Claude Code: timeout 90 s, `ANTHROPIC_BASE_URL=http://127.0.0.1:8080`,
  `ANTHROPIC_API_KEY=local` (marcador local), `CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1`,
  `CLAUDE_CODE_DISABLE_UNKNOWN_MODEL_WINDOW_ENFORCEMENT=1`,
  `TMPDIR=/home/cumplebench/runtime/tmp`.
- Primer JSON: sesión `d46ddabb-9ddf-45dd-8b83-d4b832bf0abe`, duración 512 ms,
  resultado de tres backticks; etiqueta `modelUsage` `claude-opus-5[1m]`.
- Reintento explícito local: sesión `935f40bc-c801-4736-9124-3b39db4e0b8f`,
  duración 14303 ms, 22924 tokens de entrada y 34 de salida;
  `modelUsage.local`, `permission_denials: []`, `stop_reason: end_turn`.
  Resultado: “I cannot read `.f129b-read-probe.txt` because it does not exist
  in your repository. The file you're looking for is not present.”

La inexistencia del probe es una afirmación inventada sin lectura; el cierre
no la toma como constatación del filesystem. No se atribuye causalidad al
warning `unrecognized_model`. El `contextWindow` mostrado por el cliente no
sustituye los 32768 tokens del servidor.

No hay run RAT para F1.29B. Este documento registra evidencia reportada; no
afirma una nueva verificación del probe ni hashes de GGUF/template. Los logs,
el template original y el patch externo se conservan como historia; este
cierre no modifica ni reconstruye esos artefactos.
